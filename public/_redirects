# Cloudflare redirects (static export, served by Workers static assets).
#
# The www -> bare domain redirect lives in the Cloudflare dashboard
# (Rules -> Redirect Rules), because this file only accepts paths, not hosts.
# Trailing slashes are handled automatically by Cloudflare's default
# html_handling ("auto-trailing-slash"), so no rule is needed here.

# --- Old URLs ---------------------------------------------------------------
# The privacy page moved to /privacy-policy when the legal pages were added.
/privacy /privacy-policy 301
